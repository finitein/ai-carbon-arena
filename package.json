{
  "name": "carbon-silicon-arena",
  "version": "0.1.0",
  "private": true,
  "description": "碳硅竞技场 — AI Agent 对战平台",
  "scripts": {
    "build": "pnpm --filter @carbon-arena/engine build && pnpm --filter @carbon-arena/protocol build && pnpm --filter @carbon-arena/server build && pnpm --filter @carbon-arena/web build",
    "build:server": "pnpm --filter @carbon-arena/engine build && pnpm --filter @carbon-arena/protocol build && pnpm --filter @carbon-arena/server build",
    "dev:server": "pnpm --filter @carbon-arena/server dev",
    "dev:web": "pnpm --filter @carbon-arena/web dev",
    "dev:all": "pnpm --filter @carbon-arena/server dev & pnpm --filter @carbon-arena/web dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint 'packages/*/src/**/*.ts'",
    "lint:fix": "eslint 'packages/*/src/**/*.ts' --fix",
    "format": "prettier --write 'packages/*/src/**/*.ts'",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.0",
    "typescript": "^5.4.0",
    "vitest": "^1.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
